# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit
.env
firebase-key.json
*-firebase-adminsdk-*.json

# Docker local-dev override — machine-specific, never commit
docker-compose.override.yml
